<%= form_for @aws_account, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :access_key, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :access_key, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :secret_key, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :secret_key, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                aws_accounts_path, :class => 'btn' %>
  </div>
<% end %>
